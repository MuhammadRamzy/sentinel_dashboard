/** @type {import('postcss-load-config').Config } */
const config = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {}, // Added from postcss.config.js
    // Add any other plugins you need here monu
  },
};

export default config;
